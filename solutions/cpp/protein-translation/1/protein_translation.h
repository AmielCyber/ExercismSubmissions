#pragma once
#include <string>
#include <vector>

namespace protein_translation {
std::string get_protein(std::string &);
std::vector<std::string> proteins(std::string);
} // namespace protein_translation
