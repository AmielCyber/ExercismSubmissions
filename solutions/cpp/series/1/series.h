#pragma once
#include <string>
#include <vector>

namespace series {
std::vector<std::string> slice(const std::string &digits, const int length);

// TODO: add your solution here

} // namespace series
